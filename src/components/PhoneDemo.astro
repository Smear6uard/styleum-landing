---
// PhoneDemo.astro - 3-phone timeline: notification → cards → chosen
---

<section id="phone-demo" class="relative z-30 -mt-16">
  <div class="bg-white rounded-t-[3rem] pt-20 pb-32">
    <div class="max-w-6xl mx-auto px-6 lg:px-12">

      <!-- Caption (upper left on desktop, left-aligned on mobile) -->
      <div class="mb-12 lg:mb-16 lg:max-w-md">
        <p class="text-xs uppercase tracking-[0.2em] text-black/40 font-semibold mb-3">
          See it happen
        </p>
        <h2 class="font-display text-2xl sm:text-3xl font-bold text-[#111111] leading-tight mb-3">
          Your morning, solved.
        </h2>
        <p class="text-black/50 text-base">
          Every morning at 9AM. Your wardrobe, finally working for you.
        </p>
      </div>

      <!-- 3 Phones in horizontal row -->
      <div class="flex gap-4 sm:gap-6 lg:gap-10 overflow-x-auto snap-x snap-mandatory pb-4 -mx-6 px-6 lg:mx-0 lg:px-0 lg:overflow-visible lg:justify-center">

        <!-- Phone 1: Notification -->
        <div class="flex-shrink-0 w-[160px] sm:w-[180px] lg:w-[220px] snap-center">
          <div class="bg-[#1a1a1a] rounded-[1.5rem] sm:rounded-[2rem] p-[2px] shadow-[0_12px_40px_rgba(0,0,0,0.15)]">
            <div class="aspect-[9/19.5] bg-[#0a0a0a] rounded-[1.4rem] sm:rounded-[1.8rem] overflow-hidden relative">
              <!-- Placeholder for real media -->
              <div class="absolute inset-0 flex items-center justify-center">
                <span class="text-white/20 text-xs">9:00 AM</span>
              </div>
            </div>
          </div>
          <!-- Label below phone -->
          <p class="text-center text-xs text-black/40 mt-3 font-medium">Notification</p>
        </div>

        <!-- Phone 2: Outfit Cards -->
        <div class="flex-shrink-0 w-[160px] sm:w-[180px] lg:w-[220px] snap-center">
          <div class="bg-[#1a1a1a] rounded-[1.5rem] sm:rounded-[2rem] p-[2px] shadow-[0_12px_40px_rgba(0,0,0,0.15)]">
            <div class="aspect-[9/19.5] bg-[#0a0a0a] rounded-[1.4rem] sm:rounded-[1.8rem] overflow-hidden relative">
              <!-- Placeholder for real media -->
              <div class="absolute inset-0 flex items-center justify-center">
                <span class="text-white/20 text-xs">9:01 AM</span>
              </div>
            </div>
          </div>
          <!-- Label below phone -->
          <p class="text-center text-xs text-black/40 mt-3 font-medium">Pick your look</p>
        </div>

        <!-- Phone 3: Chosen Outfit -->
        <div class="flex-shrink-0 w-[160px] sm:w-[180px] lg:w-[220px] snap-center">
          <div class="bg-[#1a1a1a] rounded-[1.5rem] sm:rounded-[2rem] p-[2px] shadow-[0_12px_40px_rgba(0,0,0,0.15)]">
            <div class="aspect-[9/19.5] bg-[#0a0a0a] rounded-[1.4rem] sm:rounded-[1.8rem] overflow-hidden relative">
              <!-- Placeholder for real media -->
              <div class="absolute inset-0 flex items-center justify-center">
                <span class="text-white/20 text-xs">9:02 AM</span>
              </div>
            </div>
          </div>
          <!-- Label below phone -->
          <p class="text-center text-xs text-black/40 mt-3 font-medium">Wear it</p>
        </div>

      </div>

    </div>
  </div>
</section>
